import React from 'react';

const ProjectBlock = ({ heading, image, link, description }) => {
    return (
        <div className="section teal">
            <h1>I am a project block.</h1>
        </div>
    )
}

export default ProjectBlock
